# Development dependencies: the pinned runtime set, plus everything needed to
# test, lint, format, compile locks, and package the desktop build. Not
# shipped inside the app or the container image.
-r requirements.txt
pytest>=8.0
pip-tools>=7.4
briefcase>=0.4.5
# Pinned exactly, not >=: this branch's lint/format history has no version
# of record before now, so the point of pinning is to fix the tool this
# branch was actually validated with, not just any later release of it.
ruff==0.16.6
black==26.5.1

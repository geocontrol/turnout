services:
  turnout:
    build: .
    ports: ["8100:8100"]
    volumes: ["./data:/app/data"]
    environment:
      TURNOUT_DB: /app/data/turnout.db
      # TURNOUT_TOKEN: set this to require a token on the organiser routes
    restart: unless-stopped
